export default {
  namespace: "comment",
  state: {},
  reducers: {
      addComment(state, action){

      },
      deleteComment(state, action){

      },
      editComment(state, action){

      },
      updateInput(state, action){
          
      }
  }
};
